# GZ Intelligence v1.0.2 — Comprehensive Security Remediation

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Address all 50 remaining security findings from the audit that are within our power to fix.

**Repo:** `~/Documents/GrowthZone-Github/gz-anything-llm` branch `master`
**Node:** `/opt/homebrew/opt/node@20/bin`

---

## Batch 1: Server Hardening (6 fixes — server/index.js + middleware)

### 1A: Body parser 3GB → 10MB
File: `server/index.js` — find the body parser config and reduce limit.

### 1B: Add security headers via helmet
Install helmet, add as middleware in server/index.js.

### 1C: Add rate limiting via express-rate-limit
Install express-rate-limit, add global rate limiter (100 req/15min per IP).

### 1D: Restrict CORS origins
Replace `origin: true` with explicit localhost origins.

### 1E: Add Multer file size limits
File: `server/utils/files/multer.js` — add `limits: { fileSize: 100 * 1024 * 1024 }` (100MB).

### 1F: WebSocket connection limits
File: `server/index.js` or `server/endpoints/agentWebsocket.js` — add max connections per IP.

---

## Batch 2: Our OAuth Code Cleanup (3 fixes)

### 2A: Client ID — remove base64 obfuscation
File: `server/utils/AiProviders/anthropic/oauth.js` — replace Buffer.from(base64) with plain string constant.

### 2B: Generate strong .env secrets on first run
File: `server/utils/helpers/updateENV.js` — when dumping env, generate crypto-random JWT_SECRET/SIG_KEY/SIG_SALT if they're still the default weak values.

### 2C: Sanitize token exchange error messages
File: `server/utils/AiProviders/anthropic/oauth.js` — don't include raw response body in error messages.

---

## Batch 3: SQL/Injection Hardening (3 fixes)

### 3A: SQL Agent — SELECT-only whitelist
File: `server/utils/agents/aibitat/plugins/sql-agent/query.js` — validate LLM-generated SQL starts with SELECT, reject DDL/DML.

### 3B: Markdown XSS — disable html:true
File: `frontend/src/utils/chat/markdown.js` — set `html: false` in markdown-it config.

### 3C: Agent Flow SSRF — validate URLs
File: `server/utils/agentFlows/executors/api-call.js` — block localhost/private IPs.

---

## Batch 4: MCP Security (3 fixes)

### 4A: Filter environment variables passed to MCP servers
File: `server/utils/MCP/hypervisor/index.js` — only pass explicitly declared env vars from MCP config, not process.env.

### 4B: MCP process resource limits
File: `server/utils/MCP/hypervisor/index.js` — add timeout and memory limits to spawned processes.

### 4C: MCP command validation
File: `server/utils/MCP/hypervisor/index.js` — validate command paths exist and are executables, block shell metacharacters.

---

## Batch 5: Dependency CVE Fixes (npm audit)

### 5A: Run npm audit fix across all packages
Server, frontend, desktop — fix what's auto-fixable.

### 5B: Manually update critical packages
- convict (prototype pollution)
- multer (DoS)
- @modelcontextprotocol/sdk (data leak + ReDoS)
- @aws-sdk/* chain (xml-parser)
- axios (SSRF)
